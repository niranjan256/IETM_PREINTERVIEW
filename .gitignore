# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
.eggs/
dist/
build/
*.spec
venv/
.ipynb_checkpoints/
*.ipynb
*.docx

# ── Django ────────────────────────────────────────────────────────────────────
backend/staticfiles/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
*.env
!.env.standalone.example
!.env.standalone

# ── Node / frontend ───────────────────────────────────────────────────────────
node_modules/
dist/
.vite/
*.local

# ── Build artifacts / packages ────────────────────────────────────────────────
IETM_Standalone/
IETM_Standalone.zip
frontend.zip
frontend (2).zip
*.zip

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Claude Code assistant files ──────────────────────────────────────────────
.claude/
.sixth/
.gemini/

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.user

# ── Docker ────────────────────────────────────────────────────────────────────
.env.docker
nginx/certs/
rename_frontend.ps1

# ── Deployment artifacts (regenerable) ───────────────────────────────────────
dist/
_trash/
ietm_offline_bundle/
finetune/lora_*/
finetune/dataset.jsonl

# ── Django runtime data ────────────────────────────────────────────────────────
backend/db.sqlite3
backend/db.sqlite3-journal
backend/db.sqlite3.bak*
backend/chroma_db/
backend/media/

# ── Pipeline output ───────────────────────────────────────────────────────────
pipeline_updated/ietm_new/

# ── Large binaries / installers / model weights (do not commit to GitHub) ────
*.exe
*.tar
*.msi
*.dmg
*.iso
*.traineddata
# Ollama model store
**/bin/models/
**/bin/ollama*
# Electron / packaged builds
Image Upload Form Design (1)/bin/
Image Upload Form Design (1)/release/
Image Upload Form Design (1)/dist-bundle/
Image Upload Form Design (1)/dist-electron-build/
# Docker image tarballs
docker-deployment/ietm-docker-images.tar
# Database seed dumps (may contain sensitive document IDs / user hashes)
docker-deployment/seed_data.json
**/seed_data.json

# ── Restricted document content (CALM_* technical manuals) — never commit ────
pipeline_updated/
frontend/public/home-bg.mp4
frontend/public/examples/
