
  # Offline SPA with Navigation

  This is a code bundle for Offline SPA with Navigation. The original project is available at https://www.figma.com/design/P0adaa7reAl0JphQMbKxpV/Offline-SPA-with-Navigation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  